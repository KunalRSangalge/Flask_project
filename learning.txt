
using the jinja template to get variables and adding logical statements in the html file
{{}} -> variables
{%%} -> logical statements



TEMPLATE INHERITANCE - 
from base.html inheriting in home.html

in home.html
{% extends 'base.html' %}
{% block NAME_OF_BLOCK %}
our content unique to home.html
{% endblock %}

in base.html, wherever the unique code has to come ther we will write
{% block NAME_OF_BLOCK %}
{% endblock %}

navigating to another page:
href="/home" -> ok but hardcoding not good
href="{{ url_for('market_page') }}  -> best practise to call the function 



DATABASE - SQLAlchemy , sqlite
uniform resource identifier(URI) 

in the python shell:
>>> from market import app, db

>>> with app.app_context():
...     db.create_all()

>>> item1 = Item(name="iphone16",price=1000,barcode="121212121212",description="hello lol")

>>> with app.app_context():
...     db.session.add(item1)
...     db.session.commit()

>>> with app.app_context():
...     Item.query.all()


you can write all the sql code in a python (seed.py)
then run all 


RESTRUCTURING:
will create all files neatly
prevent circular imports -> packaging __init__
python runs __init__ before any other

FORMS:
using forms to collect all details in register.html
creating a secret key
>>> os.urandom(12).hex()
'cfc097cbfff76668a99ec072'

handling forms with methods GET, POST

(CSRF) CROSS SITE REQUEST FORGERY
attacks to the database

solution: in register.html
{{ form.hidden_tag() }}

Done flask validations with built in classes for registration process

USER AUTHENTICATION:
hashing passwords using getter and setter

Login: flask-login

check if username is same in database

Exception: Missing user_loader or request_loader. Refer to http://flask-login.readthedocs.io/#how-it-works for more info.
for login we need the following clases specified in user class:
is_authenticated
is_active
is_anonymous
get_id

but, we will use the mixins class which has all these, then inherit

After this, make frontend changes for better user experience

FIXING LOGIN:
access of market page only after login
solved using login_manager
but after creating/registering acc -> need to login same -> direct login
fixed that

MODALS: bootstrap
focuses on looks and all 
beautify market 

fixing MoreInfo button - item_modal unique for every item and checking description
fixing the purchaseitem button - form for purachasing current item